# Watermark

Place a transparent `.png` or `.webp` channel mark here. The workflow uses
`channel-watermark.png` when present and otherwise draws the validated
`watermark_text` in the upper-right safe area. Keep artwork licensed and
reasonably high-resolution.

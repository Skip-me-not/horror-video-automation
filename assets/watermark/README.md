# Watermark

Place a transparent `.png` or `.webp` channel mark here. The workflow uses
`channel-watermark.png` when present and otherwise renders without one. Keep the
artwork licensed and reasonably high-resolution.
